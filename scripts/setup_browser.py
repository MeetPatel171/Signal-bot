"""Launch a persistent Chromium for manual Discord login.

Run once: `make setup-browser`. Log into Discord in the window that opens,
then close it. Session persists in `data/browser-profile/`.

Implementation lands in build step 5 (plan §9).
"""
